# Core MCP
fastmcp>=0.5.0

# HTTP Client (최신 버전, HTTP/2 지원)
httpx[http2]>=0.27.0

# 환경 변수 관리
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# 성능 최적화 (선택적)
uvloop>=0.19.0; sys_platform != 'win32'  # Unix 계열에서만 설치
